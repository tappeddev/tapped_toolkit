name: tapped_toolkit
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=3.3.1"

dependencies:
  flutter:
    sdk: flutter

  after_first_build:
    path: packages/after_first_build

  base_text_field:
    path: packages/base_text_field

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  tapped_lints:
    git:
      url: https://github.com/tappeddev/tapped_lints.git
      ref: master

flutter:
